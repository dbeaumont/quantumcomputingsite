services:
  # Development service
  quantum-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: quantum-computing-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
      - WATCHPACK_POLLING=true
    profiles:
      - dev

  # Production service
  quantum-prod:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: quantum-computing-prod
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    profiles:
      - prod
